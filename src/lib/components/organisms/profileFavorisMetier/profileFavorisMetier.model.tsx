import ProfileFavorisMetierView from "./profileFavorisMetier.view";

const ProfileFavorisMetierModel: React.FC = () => {
  return <ProfileFavorisMetierView />;
};

export default ProfileFavorisMetierModel;
